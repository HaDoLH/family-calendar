# 行程表 專案進度

最後更新：2026-09-24

> ⚠️ 這個 repo 是**公開的**。家庭代碼等於資料的鑰匙，**不要寫進這個檔案或 repo 內任何地方**。
> 代碼在自己的書籤和 Claude 的本機記憶裡。

## 目前狀態

已上線，兩人（呆男友／憨女友）實際在使用。網址 `https://hadolh.github.io/family-calendar/`（要帶 `#f=家庭代碼`）。

**功能全部完成並實機驗證過：**

- 中文一句話解析成行程；一次貼多筆自動拆開
- 歸屬關鍵字（`女友` `共同` `我`…）位置自由、分隔符號可有可無
- 個人與共同分色；點行程可編輯（標題／日期／時間／歸屬）；加照片
- PWA 全螢幕、自訂圖示、加到主畫面
- **手機推播**：前一晚 21:00 推明天、早上 09:00 推今天，沒行程不推
- **App 圖示徽章**：收到推播掛數字，打開才消失
- **自動更新**：回到前景比對 ETag，有新版自動重新載入

## 下一步

- [ ] `icon-512.png` 是由 180×180 放大的，偏軟。有原稿可重新輸出（只影響 Android，iPhone 看不到）
- [ ] 匯出 `.ics` 單筆到 iOS 行事曆
- [ ] iOS 行事曆訂閱（webcal）— 需要能持續產生 `.ics` 的服務，GitHub Pages 是靜態的做不到，打算用 Cloudflare Workers 讀 Firestore REST API
- [ ] LINE 每日通知：計畫已核准但程式未寫，使用者說先緩。排程可與手機推播共用同一套 GitHub Actions
  - 時間同上；推到兩人群組。⚠️ LINE Notify 已於 2025/3/31 終止，要用 Messaging API（免費 200 則/月）
- [ ] 雙向：直接在 LINE 群組打「新增9/15 看醫生」。需要一個能接 Webhook 的網址，`parse()` 可整段搬過去
- [ ] 要開放給更多人時，升級成 Google 登入 + 白名單

## 推播的運作方式

```
GitHub Actions 定時執行（台北 09:00 / 21:00，cron 是 UTC 要減 8）
  → scripts/notify.js 讀 Firestore 篩出當日行程
  → 沒行程就結束，不送
  → web-push 推給 families/<代碼>/pushsubs 裡的每台裝置
  → sw.js 收到後顯示通知 + setAppBadge
```

- **為什麼一定要有伺服器**：手機上的 JavaScript 在 App 關閉時不會執行，所以「排定通知」做不到
- **GitHub Secrets**：`VAPID_PRIVATE_KEY`、`VAPID_SUBJECT`、`FAMILY_CODE`（VAPID 公開金鑰不是機密，寫在 `index.html` 和 workflow 裡，兩邊必須一致）
- **手動測試**：repo → Actions → 「每日行程推播」→ Run workflow，可選 today / tomorrow
- **指定日期測試**：`node scripts/notify.js 2026-10-03`
- **`keepalive.yml`**：每月 commit 一次時間戳。公開 repo 超過 60 天沒有新 commit，GitHub 會把排程**自動停用**，而且只有 commit 算活動

## 重要決定（為什麼這樣做）

- **2026-09-08**：先用 Claude Artifact 做原型。可以立刻驗證解析邏輯和介面，不用先申請任何服務。
- **2026-09-18**：搬到 GitHub Pages + Firebase Firestore。因為 Artifact 只要用了內建資料庫就會被強制設成「組織內部」，**家人必須有同組織的 Claude 帳號才打得開**，這讓「家庭共用」的核心目的不成立。
- **2026-09-18**：安全機制選「家庭代碼」而不是 Google 登入。因為要家人完全免登入（目前只有兩人用）。資料放 `families/<代碼>/`，Firestore 規則只允許代碼長度 ≥ 8，且**根層級不給任何規則，所以沒人能列舉出有哪些家庭**。已用 REST API 實測：長代碼可讀寫、短代碼 403、列舉 403。
- **2026-09-18**：代碼放在網址 `#f=` 裡而不是只存 localStorage。因為 localStorage 不保證留得住（見下方坑），而且這樣「分享連結」本身就等於分享權限。
- **顏色不存進每一筆行程**，而是畫面要畫的時候才查色票表。這樣改代表色時，過去所有行程會一起變色 —— 跟 InDesign 的色票一樣。
- **資料層與畫面刻意分開寫**。搬家時整個後端換掉，只寫了約 40 行轉接器把 Firestore 包成原本的呼叫方式，500 多行的應用程式碼一行都沒改。
- **2026-09-24**：歸屬關鍵字從「必須是行首前綴 + 分隔符號」放寬成「出現在任何位置」，但**保留「獨立成詞」這條界線**（前後必須是空白、標點或句首句尾）。因為關鍵字也會出現在標題裡 —— 「女友家族吃火鍋」的「女友」黏著「家族」、「幫女友買禮物」是自己的事不是她的事，兩者都不該被判成歸屬、也不該把字從標題挖掉。**放寬條件的同時必須找到新的界線**，完全沒有界線會開始誤判標題，而誤判比不判更難發現。
- **2026-09-24**：`matchMember()` 只接受「完全相同」或「是某個成員名字的一部分」，移除反向的 `includes`。否則「幫女友買禮物」整串會被拿去跟名字比對。
- **2026-09-24**：推播只做每日摘要，不做「開始前 30 分鐘提醒」。因為 GitHub Actions 排程實際觸發常延遲 5–15 分鐘，當不了精準鬧鐘。要做精準提醒得換 Cloudflare Workers Cron。

## 踩過的坑

- **iOS 輸入框字級小於 16px** → 點下去打字時 Safari 會自動放大整頁，畫面變得比螢幕寬，右邊的按鈕全被推出去。這是「版面跑掉」的真正原因，不是尺寸算錯。**所有輸入框一律 16px 起跳。**
- **橫向捲動的元件沒加 `overscroll-behavior: contain`** → iOS 上手勢滑到底會外溢去平移整個網頁，連瀏覽器自己的介面都會移位。
- **localStorage 在 App 內嵌瀏覽器不會留存** → 原本每台裝置自己產生身分編號，結果每開一次 App 就多一個叫「我」的成員。改成「成員是共用名冊，你只是選自己是哪一位」。
- **刪除／修改沒有做樂觀更新** → 等雲端回應才更新畫面，手機把頁面凍結後會看起來像「刪不掉」，其實雲端早就刪了。改成先動畫面、失敗才復原。
- **⚠️ PWA 會沿用舊頁面** → iOS 從主畫面打開時常直接沿用上次還在記憶體裡的頁面，不重新抓程式。結果是**修好的功能在手機上看起來沒效，而且毫無跡象** —— 曾因此誤判「歸屬判定壞了」，但線上版程式碼實跑是正確的。解法：回到前景時用 `fetch(cache:'reload')` 比對 `index.html` 的 ETag，變了就重新載入（不用自己維護版本號，檔案一改 ETag 就變）。**排查任何「手機上沒生效」的問題，第一件事就是確認手機跑的是不是最新版。**
- **iOS 推播的四個限制** → ①必須是加到主畫面的 PWA，Safari 分頁完全不支援 ②需要 iOS 16.4+ ③授權詢問只能由使用者點擊觸發，不能自動跳 ④刪掉主畫面圖示，訂閱就失效要重開。另外**每台裝置都要各自開啟一次**。
- **`requireInteraction` 在 iOS 會被忽略**（讓通知停留直到處理），那是桌機 Chrome 才支援的。改用 App 圖示徽章 + 教使用者把 iOS 的「橫幅樣式」改成「持續」。
- **做 App 圖示**：必須完全不透明（有透明區會被 iOS 合成成黑底）、不要自己切圓角（iOS 會再圓一次變雙重圓角）、檔名不要有空白。
- **中文經過 shell 傳給 curl 會被編碼弄壞** → 曾把 Firestore 的欄位值改成亂碼。要帶中文的 JSON 就用 Node 寫成 UTF-8 檔案再 `--data-binary @檔案`。

## 怎麼接手

- 開新對話說：**「繼續行程表」**
- 主要檔案：`index.html`（全部前端程式，含 CSS 與 JS）、`sw.js`（推播接收）、`scripts/notify.js`（推播發送）、`.github/workflows/`、`firebase-config.js`、`manifest.json`
- **本機測試一定要起伺服器**：`python -m http.server 8000` 然後開 `http://localhost:8000`。
  不能雙擊開 —— 程式用 `import()` 動態載入 Firebase SDK，`file://` 協定會被瀏覽器擋掉
- **改完要 push 才會生效**（線上版來自 repo，不是本機），部署要等 30～60 秒
- **查線上資料不必開瀏覽器**：用 Firestore REST API 配 `firebase-config.js` 裡的 apiKey（公開的）加家庭代碼就能讀寫，除錯很快
- **App 名稱是「行程表」，但本機資料夾（`群曆`）和 repo（`family-calendar`）仍是舊名**。改動會牽連 git 和線上網址，刻意不動
- 要準備的東西：家庭代碼（在書籤裡）；Firebase Console 需要 Google 帳號且該帳號必須已開啟兩步驟驗證
