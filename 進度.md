# 群曆 專案進度

最後更新：2026-09-24

> ⚠️ 這個 repo 是**公開的**。家庭代碼等於資料的鑰匙，**不要寫進這個檔案或 repo 內任何地方**。
> 代碼在自己的書籤和 Claude 的本機記憶裡。

## 目前狀態

已上線可用，兩人實際在使用中。網址 `https://hadolh.github.io/family-calendar/`（要帶 `#f=家庭代碼`）。

功能都完成了：中文一句話解析成行程、一次貼多筆自動拆開、行首前綴指定歸屬（`共同-`／`女友-`）、個人與共同分色、點行程可編輯、加照片、PWA 全螢幕與自訂圖示。

## 下一步

- [ ] **手機推播（Web Push）** ← 優先。使用者 2026-09-24 決定先做這個，計畫還沒開
  - 需要：`sw.js`、VAPID 金鑰（私鑰放 GitHub Secret）、訂閱存進 Firestore、GitHub Actions 定時發送
  - iOS 限制：一定要加到主畫面才有推播、權限只能由點按鈕觸發、刪掉主畫面圖示訂閱就失效、每台裝置要各自開啟一次
- [ ] **LINE 每日通知**：計畫已核准但程式未寫，使用者說先緩。排程可與手機推播共用同一套 GitHub Actions
  - 時間已定：前一晚 21:00 推「明天」、早上 09:00 推「今天」，推到兩人群組
  - ⚠️ LINE Notify 已於 2025/3/31 終止，要用 Messaging API（免費 200 則/月）
- [ ] `icon-512.png` 目前是由 180×180 放大的，偏軟。有原稿的話重新輸出一張
- [ ] iOS 行事曆訂閱（webcal）— 需要能持續產生 `.ics` 的服務，GitHub Pages 是靜態的做不到

## 重要決定（為什麼這樣做）

- **2026-09-08**：先用 Claude Artifact 做原型。可以立刻驗證解析邏輯和介面，不用先申請任何服務。
- **2026-09-18**：搬到 GitHub Pages + Firebase Firestore。因為 Artifact 只要用了內建資料庫就會被強制設成「組織內部」，**家人必須有同組織的 Claude 帳號才打得開**，這讓「家庭共用」的核心目的不成立。
- **2026-09-18**：安全機制選「家庭代碼」而不是 Google 登入。因為要家人完全免登入（目前只有兩人用）。資料放 `families/<代碼>/`，Firestore 規則只允許代碼長度 ≥ 8，且**根層級不給任何規則，所以沒人能列舉出有哪些家庭**。已用 REST API 實測：長代碼可讀寫、短代碼 403、列舉 403。
- **2026-09-18**：代碼放在網址 `#f=` 裡而不是只存 localStorage。因為 localStorage 不保證留得住（見下方坑），而且這樣「分享連結」本身就等於分享權限。
- **顏色不存進每一筆行程**，而是畫面要畫的時候才查色票表。這樣改代表色時，過去所有行程會一起變色 —— 跟 InDesign 的色票一樣。
- **資料層與畫面刻意分開寫**。搬家時整個後端換掉，只寫了約 40 行轉接器把 Firestore 包成原本的呼叫方式，500 多行的應用程式碼一行都沒改。

## 踩過的坑

- **iOS 輸入框字級小於 16px** → 點下去打字時 Safari 會自動放大整頁，畫面變得比螢幕寬，右邊的按鈕全被推出去。這是「版面跑掉」的真正原因，不是尺寸算錯。**所有輸入框一律 16px 起跳。**
- **橫向捲動的元件沒加 `overscroll-behavior: contain`** → iOS 上手勢滑到底會外溢去平移整個網頁，連瀏覽器自己的介面都會移位。
- **localStorage 在 App 內嵌瀏覽器不會留存** → 原本每台裝置自己產生身分編號，結果每開一次 App 就多一個叫「我」的成員。改成「成員是共用名冊，你只是選自己是哪一位」。
- **刪除／修改沒有做樂觀更新** → 等雲端回應才更新畫面，手機把頁面凍結後會看起來像「刪不掉」，其實雲端早就刪了。改成先動畫面、失敗才復原。
- **做 App 圖示**：必須完全不透明（有透明區會被 iOS 合成成黑底）、不要自己切圓角（iOS 會再圓一次變雙重圓角）、檔名不要有空白。

## 怎麼接手

- 開新對話說：**「繼續群曆」**
- 主要檔案：`index.html`（全部程式都在這，含 CSS 與 JS）、`firebase-config.js`、`manifest.json`、`README.md`
- **本機測試一定要起伺服器**：`python -m http.server 8000` 然後開 `http://localhost:8000`。
  不能雙擊開 —— 程式用 `import()` 動態載入 Firebase SDK，`file://` 協定會被瀏覽器擋掉。
- **改完要 push 才會生效**（線上版來自 repo，不是本機），部署要等 30～60 秒
- 要準備的東西：家庭代碼（在書籤裡）；Firebase Console 需要 Google 帳號且該帳號必須已開啟兩步驟驗證
